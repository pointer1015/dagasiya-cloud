<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pointer.user.mapper.UserAppMapper">

    <insert id="addUser" parameterType="com.pointer.user.entity.User">
        insert into user (user_name, password, phone, email,status,register_time,deleted)
        values (#{user.userName}, #{user.password}, #{user.phone}, #{user.email}, #{user.status}, #{user.registerTime}, #{user.deleted})
    </insert>


    <update id="updateLastLoginTime">
        update user set last_login_time = #{date}
        where user_id = #{userId}
    </update>


    <select id="selectByEmail" resultType="com.pointer.user.entity.User">
        select * from user
        where email = #{email}
    </select>
</mapper>